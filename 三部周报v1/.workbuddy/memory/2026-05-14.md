# 2026-05-14 日记

## 主要工作

### 新品板块HTML数据更新

完成了新品板块_4.30-5.6_v2.html的全面数据更新：

1. **KPI总览行**：
   - 在售SKU：101→**109**
   - 8日Y：27→**57**
   - 8日N：13→**22**
   - 未出单：22→**30**（有对手3 + 无对手27）
   - 已出单：40→**79**
   - 低占比新品：40→**39**

2. **品类数据(catData)**：
   - 车门系统：36 SKU，销量63，销售额$10,716.02
   - 其他：33 SKU，销量48，销售额$4,906.90
   - 机盖及附件：9 SKU，销量44，销售额$2,216.00
   - 饰条：9 SKU，销量18，销售额$875.93
   - 挡泥板：9 SKU，销量14，销售额$1,148.00
   - 车身外扩件：12 SKU，销量7，销售额$587.00
   - 牌照板支架：1 SKU，销量1，销售额$45.00

3. **分析人数据(anData)**：
   - 张潇：29 SKU，销量76，销售额$4,732.99
   - 胡煜星：25 SKU，销量53，销售额$4,468.90
   - 俞东旭：25 SKU，销量32，销售额$6,841.19
   - 朱培源：16 SKU，销量19，销售额$3,525.88
   - 王偲涵：10 SKU，销量6，销售额$428.00
   - 章鹏：4 SKU，销量9，销售额$497.89

4. **新品出单情况**：
   - 有对手SKU：43个
   - 出单率：93.0%（40/43）
   - 有对手未出单：3个
   - 无对手未出单：27个

5. **低占比新品**：更新为完整的39条记录

6. **四三累计新品**：更新为完整的109条记录

### 修复环比数据和 PLG 数据

1. **数据周期显示**：从自动计算 5/14-5/20 覆盖为硬编码 `4/30 - 5/6`
2. **上周 KPI 环比数据**：
   - 向 `corrected_data.json` 新增 `prevWeekKpi` 字段
   - 上周总SKU：90（vs 本周101，+12.2%）
   - 上周总销量：151（vs 本周195，+29.1%）
   - 上周总销售额：$17,688.39（vs 本周$20,494.85，+15.9%）
   - 上周分析及时率：64.4%（vs 本周70.3%，+9.2%）
   - 上周出单数：68（vs 本周79）
3. **PLG 费率统计**：
   - 向 `corrected_data.json` 新增 `plgStats` 字段
   - PLG开启：24个（低占比39个中）
   - PLP未开且未出单：8个
   - PLP未开但PLG费率>0%：23个
   - PLP开启：15个
4. **品类/分析人维度上周数据**：原始数据源不包含按品类/分析人维度的上周明细，显示 "--"
5. **构建验证**：npm run build 成功

## 技术问题

- **PowerShell编码问题**：PowerShell脚本在处理UTF-8中文内容时出现乱码，导致HTML文件被损坏（只有3字节）
- **解决方案**：使用Python脚本重新生成完整的HTML文件

### PLG 费率明细表 + 筛选栏

在广告追踪 Tab 的「PLG 费率统计」区域新增了完整功能：

1. **数据层修复**：
   - `newProductStatusAdapter.ts` 中 `plgData.records` 从空数组改为基于 `lowShareData` 生成 39 条 PLGRecord
   - `corrected_data.json` 的 `plgStats` 重新计算，字段名对齐用户定义的三个条件
   - 新字段：`plpAndPlgBothCount`(1)、`plgOnlyCount`(23)、`plpDisabledNoSaleCount`(9)

2. **UI 筛选栏**：4个筛选按钮（全部39/PLP+PLG同时开启1/单开PLG 23/PLP未开且未出单9）
   - KPI 卡片可点击触发筛选
   - 筛选按钮高亮 + 清除筛选功能

3. **明细数据表**：14列（序号、SKU、分析人、品类、拓展类型、PLP、PLG费率、销量、销售额、对手出单、市占比、8日出单、操作状态、市场状态）
   - 三种条件对应行背景色高亮（绿/琥珀/红）
   - 底部显示筛选记录数

4. **接口调整**：`PLGData` 接口字段 `plgEnabledCount` → `plpAndPlgBothCount`，`plpDisabledPlgActiveCount` → `plgOnlyCount`

### PLG 明细补全（39→101 条）

用户反馈 PLG 明细新品数量不全，原数据仅包含 lowShareData（39条低占比新品）。

1. **新增 `plgRecords` 数组**：以 `cum43Data`（101条全部新品）为基础，合并 `lowShareData` 的 PLP/PLG 字段，缺失的默认 PLP=N、PLG=0%
2. **plgStats 更新**：
   - 全部：101 → PLP+PLG同时开启：1 → 单开PLG：23 → PLP未开且未出单：35（原9）
   - PLP开启：15，PLP未开：86
3. **adapter 修改**：`plgData.records` 改为从 `plgRecords` 加载，`totalLowShare` → `totalNewProducts`

### PLG 费率修正 + 新增筛选条件

用户指出 PLG 费率有问题，以 Excel「新品PLG」sheet 为权威来源修正。

1. **数据修正**：从 Excel 读取全部 101 条 PLG/PLP 数据，发现 50 条记录的 PLP/PLG 值与 JSON 不一致
   - 原因：之前 `plgRecords` 对非低占比新品（62条）默认填 PLP=N、PLG=0%，但实际 Excel 中这些新品有真实的 PLP/PLG 开启状态
   - 修正后统计数据：PLP+PLG同时开启=7，单开PLG=41，单开PLP=40，无广告=13，PLP未开且未出单=16

2. **新增两个筛选条件**：
   - 单开PLP（PLP=Y且PLG费率=0%）：40条
   - 无广告（PLP=N且PLG费率=0%）：13条

3. **筛选栏升级为 5+1（全部）模式**：KPI 卡片行 5 列 + 筛选按钮 6 个
   - 行背景色：绿=PLP+PLG，琥珀=单开PLG，蓝=单开PLP，灰=无广告，红=未开且未出单

4. **同步修正 `lowShareData` 中的 PLP/PLG 字段**

## 文件清单

- `新品板块_4.30-5.6_v2.html`：重新生成的完整HTML报告（约100KB）
- `corrected_data.json`：包含109条四三累计数据和39条低占比新品数据

---

- **2026-05-14 下午**：修复 PLG 明细「PLP未开且未出单」筛选逻辑

### 修复内容
- 筛选条件从 `r.curSalesQty === 0`（销量为0）改为 `r.firstSaleDate === '未出单'`
- 之前会把 8 日出单为 "N" 的产品（上架超8天但当周无销量）也筛入
- 修正后只筛出 `firstSaleDate === '未出单'` 的产品（PLP未开 + 真正未出单）
- `plpDisabledNoSaleCount`：16 → **3**
- 修改文件：`NewProductStatusPage.tsx`（行3175筛选条件）、`corrected_data.json`（plpStats.plpDisabledNoSaleCount）

---

- **2026-05-14 下午**：多模块大更新（5个子任务）

### 1. 多维度分析新增上周销售额和销售额环比
- 品线维度表格：新增"上周销售额"、"销售额环比"、"有对手SKU"三列
- 分析人维度表格：新增"上周销售额"、"销售额环比"两列
- 数据来源：`新品周报数据_4.30-5.6.xlsx` 的品线维度和分析人维度 sheet

### 2. 核心指标总览和数据可视化新增销售额
- 核心指标总览：新增"总销售额"KPI卡片（$20,494.85，环比+15.9%）
- 总销量KPI卡片helper改为显示上周销量和环比
- 数据可视化：新增"品线销售额对比"和"分析人销售额对比"两个BarChart

### 3. PLP广告总览：ACOS替换为PLP广告销售额
- KPI行（6列）：ACOS → PLP广告销售额（$1,754.44，环比-48.4%）
- 分析人维度PLP表：ACOS列 → 广告销售额列（含环比）
- 品线维度PLP表：同上
- 数据来源：`新品周报数据_4.30-5.6.xlsx` 的新品PLP sheet（col8=销售额）

### 4. 品线维度PLP板块后附上PLP明细
- 新增PLP广告明细表（53条SKU级广告活动记录）
- 数据来源：`新品检查周源数据和PLP数据.xlsx` 的PLP明细sheet（4.30-5.6周期）
- 字段：SKU、广告活动、分析人、品类、拓展类型、曝光、点击、售出、花费、广告销售额、总销售额、ROAS、ACOS、ACOAS

### 5. PLG明细合并到四三累计
- 四三累计新品追踪明细表新增PLP和PLG费率两列
- 新增"PLG条件"筛选下拉框（5个选项：PLP+PLG同时开启/单开PLG/单开PLP/无广告/PLP未开且未出单）
- cum43Data 与 plgRecords 通过 SKU 匹配合并
- adapter 新增 PLPDetailRecord 接口、plpSummaryData/plpDetailData 数据解析

### 修改文件清单
- `corrected_data.json`：新增 categoryRevenueData(8行)、analystRevenueData(7行)、plpSummaryData(21行)、plpDetailData(53行)
- `newProductStatusAdapter.ts`：新增接口(PLPDetailRecord)、更新品线/分析人/PLP数据解析、总体KPI使用Excel真实数据
- `NewProductStatusPage.tsx`：品线/分析人表格新增列、KPI卡片新增销售额、图表新增2个、PLP替换ACOS为销售额、新增PLP明细表、四三累计新增PLG列和筛选
- 构建通过（✓ built in 7.62s）

### 修复未出单原因分析维度数据错误

**根因**：`NewProductStatusPage.tsx` 中"新品未出单原因分析"的分析人维度和品线维度表格使用了**硬编码**的 `unorderAnData` 和 `unorderCatData`（有对手列全部为0），而非从 `data.unsoldReason` 动态读取。

**修复内容**：
- 删除 `unorderAnData`（6条硬编码，a_jz/a_ws/a_total 全为0）和 `unorderCatData`（5条硬编码，同全0）
- 改为在组件内 `data` 定义后动态构建：合并 `hasCompetitor.byAnalyst/byCategory` + `noCompetitor.byAnalyst/byCategory`
- 标题行汇总文本也改为动态：`data.unsoldReason.hasCompetitor.total` + `data.unsoldReason.noCompetitor.total`
- 构建通过（✓ built in 5.89s），类型检查无错误

### 四三累计板块 UI 优化（4项任务）

1. **去除 PLG 费率明细列表**：删除广告追踪 Tab 中整个"PLG 费率统计"section（含 KPI 行、筛选栏、14列明细表），因已合并到四三累计明细表。同时删除 `plgFilter` state。

2. **新增筛选条件**：
   - 市占比筛选：高市占(≥75%)、中市占(50%~75%)、低市占(<50%)
   - 市场竞争：有竞争(对手销量≠0)、无竞争(对手销量=0)
   - 在 `cum43FilteredData` filter 中加入逻辑

3. **PLG条件→广告条件**：label 改名，state key `plgCondition` 不变

4. **筛选 UI 优化**：
   - 网格布局从 5列 改为 4列×2行，更紧凑
   - 标题行右侧添加"✕ 取消筛选"按钮（hover 变灰效果）
   - 筛选结果标签栏：每个激活的筛选条件都有独立标签（紫色），点击可单独取消
   - select 添加 focus:ring 交互效果

- 构建通过（✓ built in 5.55s），类型检查无错误

### 汇报输出模块重写

**改动**：将汇报输出 Tab 从硬编码静态文案改为动态数据驱动。

1. **汇报概览 KPI 行**（新增）：6 个指标卡片（在售SKU、总销量、总销售额、出单率、及时率、低占比新品），数据从 `data.overall.kpi` 等读取
2. **风险预警**（动态生成 `buildDynamicAnomalies`）：
   - 有对手未出单 → 高风险
   - 低占比新品≥30个 → 中风险
   - PLP广告 ACOS>15% → 中风险
   - 及时率<70% → 中风险
3. **主要发现**（动态生成 `buildMainFindings`）：6 条，覆盖出单率、销量、品类TOP、及时率、分析人亮点、PLP广告
4. **下周动作**（动态生成 `buildNextActions`）：针对未出单、低占比无市场、及时率低、广告效果差、品类增长等分别给出建议
5. **可复制周报文案**（重写 `generateReportText`）：包含核心指标、风险预警、主要发现、品类维度、下周动作五部分
6. **复制按钮优化**：从 alert 改为按钮内文字反馈"✅ 已复制"

- 构建通过（✓ built in 6.32s），类型检查无错误
