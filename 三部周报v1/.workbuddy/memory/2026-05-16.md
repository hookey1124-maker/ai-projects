# 2026-05-16 工作日志

## 创建 Skill：新品周报看板生成

用户要求将新品周报 HTML 的完整生成流程沉淀为可复用的 Skill。

### Skill 位置
`~/.workbuddy/skills/新品周报看板生成/SKILL.md`（892行，35KB）

### Skill 覆盖内容
1. **数据源要求**：21 个 JSON 数据块的完整字段定义和示例
2. **数据选取规则**：周期过滤、四三累计过滤、数据校验清单、核心业务规则
3. **HTML 结构**：5 Tab 布局（总盘概览/低占比分析/广告追踪/四三累计/汇报输出），每个 Tab 的详细 DOM 结构
4. **CSS 样式**：色彩系统（10色）、组件样式（KPI/表格/标签/筛选器）、响应式、滚动条
5. **交互与动画**：Tab 切换、Hover 效果、图表动画、复制按钮、筛选器实时联动
6. **JavaScript 规范**：数据嵌入方式、工具函数、IIFE 隔离、反引号模板字符串、延迟图表初始化
7. **生成流程**：Python 脚本模板、JS 语法验证方法
8. **常见问题排错**：JS 语法错误、数据显示异常、编码问题

### 技术要点
- Python f-string 中 `{}` 需双写为 `{{}}`
- JS 中换行符必须用 `\\n`（不能裸 `\n`）
- Chart.js 图表必须在 Tab 可见时初始化
- PLG 匹配通过 plgRecords 构建 plgMap 实现
